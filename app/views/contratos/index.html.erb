<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="app/stylesheet/main.css">
  <!-- Add any additional styles or meta tags here -->
</head>
<body class="container mt-5">

  <h1 class="text-center">Mini core de montos</h1>

  <h2 class="mt-4">Tabla de contratos</h2>

  <div class="table-responsive">
    <table class="table table-bordered">
      <thead class="thead-dark">
        <tr>
          <th>Contrato</th>
          <th>Monto</th>
          <th>Fecha</th>
          <th>Cliente</th>
        </tr>
      </thead>
      <tbody>
        <% @contratos.each do |contrato| %>
          <tr>
            <td><%= contrato.nombre %></td>
            <td><%= contrato.monto %></td>
            <td><%= contrato.fecha %></td>
            <td><%= contrato.cliente.nombre %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  
  <h2 class="mt-4">Filtrar por fecha</h2>

  <%= form_with url: contratos_path, method: :get, class: "form-inline mb-3" do |form| %>
    <%= form.date_field :inicio, class: "form-control mr-2" %>
    <%= form.date_field :fin, class: "form-control mr-2" %>
    <%= form.submit "Submit", class: "btn btn-primary" %>
  <% end %>

  <h2 class="mt-4">Total por cliente</h2>

  <div class="table-responsive">
    <table class="table table-bordered">
      <thead class="thead-dark">
        <tr>
          <th>Cliente</th>
          <th>Monto</th>
        </tr>
      </thead>
      <tbody>
        <% @totales.each do |total| %>
          <tr>
            <td><%= total.cliente_nombre %></td>
            <td><%= total.monto %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
